import React from 'react';
import { CardContent } from '@material-ui/core';

const NotFoundPage: React.FC = () => {
  return <CardContent>お探しのページが見つかりませんでした。</CardContent>;
};

export default NotFoundPage;
